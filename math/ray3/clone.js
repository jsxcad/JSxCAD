/**
 * Create a clone of the given 3D line.
 *
 * @param {line3} line - line to clone
 * @returns {line3} clone of the line
 */
const clone = ([origin, unitDirection]) => [origin, unitDirection];

module.exports = clone;
