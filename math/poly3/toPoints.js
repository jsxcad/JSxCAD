/**
 * Returns the polygon as an array of points.
 * @param {Polygon}
 * @returns {Points}
 */

// TODO: Should we repeat the first point to construct a closed path?

const toPoints = polygon => polygon;

module.exports = toPoints;
