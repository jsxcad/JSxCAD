md`
# Shell Operations`;

md`## path.inset(amount, step, limit)`;

Arc(10)
  .op((s) => s.and(s.inset(0.25, 0.5, 2.5).color('blue')))
  .topView();

md`## path.offset(amount, step, limit)`;

Arc(10)
  .op((s) => s.and(s.offset(0.25, 0.5, 2.5).color('blue')))
  .topView();